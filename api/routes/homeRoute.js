//Dependencies
const express = require('express');
const router = express.Router();

//API home route
router.get('/', (req, res) => {
  res.send('HOME Working');
})

router.get('/', (req, res) => {
  
})

module.exports = router;